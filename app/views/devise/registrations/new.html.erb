
<% if administrator_signed_in? %>
    <h2>Create new admins:</h2>





<% end %>
<% if administrator_signed_in? == false %>
    <h2>Sign up:</h2>

    <%= form_tag({:controller => :singup, :action => :do}, 'data-remote' => true) do %>
    <div class="field">
      <%= label_tag(:signup_code, 'Code:')%><br />
      <%= text_field_tag(:signup_code) %>
      <%= submit_tag("Check") %>
    </div>
    <div id="user_data">

    </div>
<% end %>



<%= render "devise/shared/links" %>

<% end %>
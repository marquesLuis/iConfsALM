<%= form_for(@rejected_contact) do |f| %>
  <% if @rejected_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rejected_contact.errors.count, "error") %> prohibited this rejected_contact from being saved:</h2>

      <ul>
      <% @rejected_contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :requester_id %><br />
    <%= f.number_field :requester_id %>
  </div>
  <div class="field">
    <%= f.label :requested_id %><br />
    <%= f.number_field :requested_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

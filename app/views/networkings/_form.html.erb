<%= form_for(@networking) do |f| %>
  <% if @networking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@networking.errors.count, "error") %> prohibited this networking from being saved:</h2>

      <ul>
      <% @networking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.number_field :person_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

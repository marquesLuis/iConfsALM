<%= form_for(@removed_notification) do |f| %>
  <% if @removed_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@removed_notification.errors.count, "error") %> prohibited this removed_notification from being saved:</h2>

      <ul>
      <% @removed_notification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sequence_number %><br />
    <%= f.number_field :sequence_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

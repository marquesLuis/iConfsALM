html = "<%= escape_javascript( render 'list' ) %>"

$("#networking_list").fadeOut("slow", function() {
    $(this).empty().append(html).fadeIn();
    $(".pagination a").attr( 'data-remote', 'true' );
})


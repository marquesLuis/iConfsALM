<h1>Listing feedbacks</h1>
<%= link_to "All", {:controller => "feedbacks", :action => "index"} %>
<%= link_to "To Read Only", {:controller => "feedbacks", :action => "index", :read=>'false'} %>
<%= link_to "Read Only", {:controller => "feedbacks", :action => "index", :read=>'true'} %></p>

<table>
  <tr>
    <th>Content</th>
    <th>Read</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @feedbacks.each do |feedback| %>
  <tr>
    <td><%= feedback.content %></td>
    <td><%= feedback.read %></td>
    <td><%= link_to 'Show', feedback %></td>
    <td><%= link_to 'Destroy', feedback, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
